export type Student = {
  id: string;
  name: string;
  nameInput: string;
  createdAt: number;
};

export type Lesson = {
  id: string;
  name: string;
  fileName: string;
  url: string;
  createdAt: number;
};
